# 🔑 ФИЛЬТРАЦИЯ РАССЫЛОК ПО КЛЮЧЕВЫМ СЛОВАМ

## 📋 Что это?

Система автоматической защиты от **повторных рассылок** по ключевым словам.

### Принцип работы:

1. **Из названия кампании** извлекаются значимые слова (≥4 буквы)
2. **Проверяется история** каждого чата
3. **Если слово найдено** в ранее отправленных сообщениях → чат **ПРОПУСКАЕТСЯ**

---

## 🎯 Пример

### Сценарий:
```
У вас есть кампания "ПОЛАТИ БЕЗ ИЗОЛИРОВКИ ПЛЮС ПАКЕТ"
```

**Шаг 1:** Извлекаются ключевые слова:
- `полати`
- `изолировки`
- `пакет`
- `плюс`

**Шаг 2:** Для каждого чата проверяется:
- Отправляли ли раньше сообщения с этими словами?
- В каких кампаниях встречались эти слова?

**Шаг 3:** Если кандидату уже писали про "ПОЛАТИ" (в любой форме):
```
✅ Ранее отправлено: "Приглашаем на работу в компанию ПОЛАТИ"
🚫 ПРОПУСКАЕМ этого кандидата
```

---

## 🛡️ Защита от дубликатов

### Игнорируется:
- **Регистр:** `ПОЛАТИ` = `полати` = `Полати`
- **Знаки препинания:** `"Полати"` = `(Полати)` = `Полати,`
- **Стоп-слова:** исключаются служебные слова (`для`, `без`, `что`, `это` и т.д.)

### Находит:
- Точное вхождение слова: `полати` в тексте `работа в компании полати`
- Слово с любыми знаками: `"полати"`, `(полати)`, `полати.`
- В любом месте: начало, середина, конец сообщения

---

## 📊 Логи

При отправке кампании в логах вы увидите:

### Извлечение ключевых слов:
```
🔑 Кампания 'ПОЛАТИ БЕЗ ИЗОЛИРОВКИ ПЛЮС ПАКЕТ' - ключевые слова: ['полати', 'изолировки', 'пакет', 'плюс']
```

### Пропуск чата (уже отправляли эту кампанию):
```
⏭️ Пропуск u2i-ABC123: кампания уже отправлена
```

### Пропуск чата (найдено ключевое слово):
```
🚫 Пропуск u2i-XYZ789: найдены ключевые слова - 'полати' в 'ПОЛАТИ', 'полати' в 'Работа в компании Полати'
```

### Разрешена отправка:
```
✅ u2i-DEF456: РАЗРЕШЕНО (нет ключевых слов в истории)
```

---

## 📈 Результаты рассылки

**API ответ:**
```json
{
  "success": true,
  "sent": 45,
  "failed": 2,
  "skipped_by_campaign": 10,
  "skipped_by_keyword": 23,
  "keywords_checked": ["полати", "изолировки", "пакет", "плюс"]
}
```

**Расшифровка:**
- `sent`: 45 - успешно отправлено
- `failed`: 2 - ошибки отправки
- `skipped_by_campaign`: 10 - уже отправляли эту точную кампанию
- `skipped_by_keyword`: 23 - **нашли ключевые слова в истории**
- `keywords_checked`: список проверенных слов

---

## 🧪 Тестирование

Запустите тест:
```bash
python test_keyword_filter.py
```

**Что проверяет:**
1. Извлечение ключевых слов из разных названий
2. Поиск слов в истории реальных чатов
3. Симуляцию фильтрации на 20 чатах

**Пример вывода:**
```
✅ Разрешено: 10 чатов
🚫 Заблокировано: 10 чатов (по ключевым словам)
```

---

## ⚙️ Настройка

### Минимальная длина слова:
По умолчанию: **4 символа**

Изменить в `messenger_app.py`:
```python
keywords = extract_keywords(campaign_name, min_length=5)  # Только слова ≥5 букв
```

### Стоп-слова:
Добавить в список `stop_words` в функции `extract_keywords()`:
```python
stop_words = {'без', 'для', 'или', 'моё_слово'}
```

---

## 🚀 Как использовать

1. **Создайте кампанию** с осмысленным названием:
   - ✅ `ПОЛАТИ БЕЗ ИЗОЛИРОВКИ ПЛЮС ПАКЕТ`
   - ❌ `Рассылка №1` (слишком общее)

2. **Выберите чаты** для отправки

3. **Отправьте кампанию** - система автоматически:
   - Извлечёт ключевые слова
   - Проверит историю каждого чата
   - Пропустит дубликаты

4. **Проверьте логи** для детальной информации

---

## 🎓 Технические детали

### Функции:

**`extract_keywords(text, min_length=4)`**
- Удаляет знаки препинания
- Приводит к нижнему регистру
- Фильтрует короткие слова
- Исключает стоп-слова
- Возвращает список уникальных слов

**`check_keyword_in_history(chat_id, keywords)`**
- Загружает историю сообщений из БД
- Ищет каждое ключевое слово
- Использует regex `\b{keyword}\b` для точного поиска слова
- Возвращает: `(bool, [(keyword, campaign_name), ...])`

### База данных:

**Таблицы:**
- `campaigns` - список кампаний
- `campaign_messages` - сообщения кампаний
- `chat_campaigns` - связь чат ↔ кампания (история отправок)

**Запрос истории:**
```sql
SELECT DISTINCT cm.text, c.name
FROM chat_campaigns cc
JOIN campaign_messages cm ON cc.campaign_id = cm.campaign_id
JOIN campaigns c ON cc.campaign_id = c.id
WHERE cc.chat_id = ?
```

---

## ✅ Готово!

Теперь вы **ЗАЩИЩЕНЫ** от повторных рассылок с одинаковыми ключевыми словами! 🛡️
